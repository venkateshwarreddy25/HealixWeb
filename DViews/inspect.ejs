<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Healix Web</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>

<body class="bg-light">
  <div class="container py-5">
    <div class="card shadow-lg">
      <div class="row g-0">
        <div class="col-md-4">
          <img src="<%= doctorview.image %>" class="img-fluid rounded-start h-100 object-fit-cover" alt="Doctor Image">
        </div>

        <div class="col-md-8">
          <div class="card-body">
            <h2 class="card-title mb-4">Doctor Profile Overview <strong><%= doctorview.DName %></strong></h2>

            <form action="/Doctors/inspect/<%= doctorview._id %>" method="POST">
              <div class="mb-3">
                <label class="form-label">Years of Clinical Experience</label>
                <input type="text" class="form-control" name="Image" value="<%= doctorview.Experience %>" required />
              </div>

              <div class="mb-3">
                <label class="form-label">Area of Specialization</label>
                <input type="text" class="form-control" name="Experience" value="<%= doctorview.Specalist %>" required />
              </div>

              <div class="mb-3">
                <label class="form-label">Medical Procedures & Techniques</label>
                <input type="text" class="form-control" name="Specalist" value="<%= doctorview.Techniques %>" required />
              </div>

              <div class="mb-3">
                <label class="form-label">Patient Communication & Care Skills</label>
                <input type="text" class="form-control" name="Patience" value="<%= doctorview.Patience %>" required />
              </div>

              <div class="d-flex gap-3">
                <a href="/Doctors/inspect/<%= doctorview._id %>/appoint" class="btn btn mb-3" style="background-color: green; color: aliceblue;">Book Appointment</a>
                <a href="/Doctors" class="btn btn mb-3" style="background-color: chocolate; color: white;">Browse All Registered Doctors</a>
              </div>
              <p><strong>Consultation Start Time :</strong> <%= new Date(doctorview.start).toLocaleString() %></p>
              <p><strong> Consultation End Time :</strong> <%= new Date(doctorview.end).toLocaleString() %></p>

            </form>

          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>